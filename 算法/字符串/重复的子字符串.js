/**
 * @param {string} s
 * @return {boolean}
 */
var repeatedSubstringPattern = function (s) {
    let str = ''
    let prefix = s
    let len = s.length
    for (let i = 1; i <= len; i++) {}
}

let s = 'abab'
console.log(repeatedSubstringPattern(s))
